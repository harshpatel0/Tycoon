import requests

server_address = "0.0.0.0"

class Update(server_address):
  def __init__(self) -> None:
    self.server_address = "0.0.0.0"
    self.request_update(server_address)
  
  self.request_update(server_address):
